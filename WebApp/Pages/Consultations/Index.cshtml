@page
@model WebApp.Pages.Consultations.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Consultation[0].IdStudent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Consultation[0].IdTeacher)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Consultation[0].ConsSubject)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Consultation[0].ConsTime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Consultation) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdStudent)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdTeacher)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ConsSubject)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ConsTime)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
